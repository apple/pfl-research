Internal API
------------

This is the reference of ``pfl.internal``.
It is not classified as a part of the public API, and don't follow the rules of `semver <https://semver.org/>`_.
We include the reference here anyway because the modules implement many useful utilities that can be reused when creating custom components.

.. toctree::
   :maxdepth: 2
   :caption: Internal API

   bisect
   bridge
   distribution
   ops
   platform
   privacy_loss_bound
   tree
